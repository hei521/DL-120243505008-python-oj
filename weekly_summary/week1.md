第一周学习总结：
1.	相对于C++语言而言，我认为python部分相同，但难点在于浮点数的精度问题，即去除小数部分，可以通过整数判断在进行取整运算(print(int(num) if num==int(num)  else num)，也可以使用字符串格式化（print(f”{num:g}”)）;以及在一系列的数据输入（list(map(int,input().split()))和一系列输出可以用逗号区别也可以append传入列表
2.	同时在python中也添加里列表推导式，能容纳不同种类对象的元组，列表切片，地板除等新型算法
3.	在选择中难点在于一系列输出和输入和精度的确定
4.	在循环中难点在于包含那个斐波那契数列（即使用range(2,n+1)中的a,b=b,a+b）以及循环中的break使用和循环条件的确定
